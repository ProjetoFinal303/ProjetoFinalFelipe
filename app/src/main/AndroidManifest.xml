<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProjetoFinal">

        <!-- Tela principal -->
        <activity
            android:name="projetofinal.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Outras atividades do app -->
        <activity
            android:name="projetofinal.main.CadastrarClienteActivity"
            android:exported="true"/>
        <activity
            android:name="projetofinal.main.VisualizarClienteActivity"
            android:exported="true"/>
        <activity
            android:name="projetofinal.main.AtualizarClienteActivity"
            android:exported="true"/>
        <activity
            android:name="projetofinal.main.ExcluirClienteActivity"
            android:exported="true"/>
        <activity
            android:name="projetofinal.main.VerProdutosActivity"
            android:exported="true"/>

    </application>

</manifest>
